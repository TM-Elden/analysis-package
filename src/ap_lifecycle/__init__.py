"""C12 package lifecycle core: supersede, recall, legal hold, retention marker, purge.

See `ap_lifecycle.workflow` for `LifecycleWorkflow`/`LifecyclePolicy` and CLAUDE.md's "C12 package
lifecycle" section for the union state machine, the purge/refcount mechanism, and the reindex-wiring
fix this task also lands.
"""
